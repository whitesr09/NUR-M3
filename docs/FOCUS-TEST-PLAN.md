# Focus tests

Verify that starting does not create a completion; pausing preserves elapsed time; resuming counts only new active time; cancelling does not create completion history; a completed session is written once; process recreation restores a paused session without credit for absence; duration validation rejects zero or excessive values; and routine editing does not alter recorded session identity. These are required checks, not a claim that tests have passed.
